# Windows runtime: reader + web app + local Kokoro/Whisper speech service + tray helper.
# PyTorch is installed separately by run-doc-reader.ps1 (CUDA build when an NVIDIA GPU
# is present, CPU build otherwise) before this file is applied.
-r requirements.txt
kokoro>=0.9.4
faster-whisper>=1.1.0
soundfile>=0.12.1
sounddevice>=0.5.0
numpy
pyttsx3>=2.90
